# Core dependencies (required)
requests>=2.28.0
beautifulsoup4>=4.11.0

# Optional: headless-browser rendering (needed only when using --render)
# Install with:
#   pip install playwright && playwright install
# playwright>=1.30.0
